<%= form_with(model: todo_model) do |form| %>
  <% if todo_model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(todo_model.errors.count, "error") %> prohibited this todo_model from being saved:</h2>

      <ul>
        <% todo_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :task, style: "display: block" %>
    <%= form.text_field :task %>
  </div>

  <div>
    <%= form.label :isCompleted, style: "display: block" %>
    <%= form.check_box :isCompleted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
